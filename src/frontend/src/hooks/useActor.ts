// useActor is not used in this project.
// The app accesses the backend directly via backendActor.ts.
// This stub exists only to prevent build errors from legacy imports.
export function useActor() {
  return { actor: null, isFetching: false };
}
